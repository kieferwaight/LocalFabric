"""
mcp.servers — FastMCP server exposing local router tools to Claude Desktop and VS Code Copilot.

Entry point: server.py
Run with:   python -m mcp.servers.server
Or via MCP: configure claude_desktop_config.json (see config.py)
"""
