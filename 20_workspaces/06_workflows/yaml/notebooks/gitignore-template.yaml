- id: notebook/git/gitignore
  title: Gitignore Template Notebook
  description: |
    Writes the standard Python `.gitignore` template in the active workspace.
  tags: [notebook, git, templates]
  extends: builtin/git/gitignore-python
